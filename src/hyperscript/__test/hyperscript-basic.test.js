var lazy = require('lazy-assert');

/* global before, beforeEach, after, afterEach */
describe('', function () {
    before(function () {
        lazy.setLocation(__filename);
    });

    beforeEach(function () {
    });

    after(function () {
    });

    afterEach(function () {
    });

    it('hyperscript basic', function () {

    });
});
